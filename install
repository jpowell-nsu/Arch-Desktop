#!/bin/sh

cp dots/.* ~/ & cp -r dots/neomutt ~/.config & cp -r dots/mpv ~/.config &&
if [ ! -d ~/Media ]; then
    mkdir ~/Media
fi &&

if [ ! -d ~/Media/Wallpapers ]; then
    cp -r Wallpapers ~/Media
else
    cp -r Wallpapers/* ~/Media/Wallpapers
fi &&

cp -r Wallpapers ~/Media &
cd suckless/dmenu &&  make && sudo make install clean &&
cd ../dwm &&  make && sudo make install clean &&
cd ../slock && make && sudo make install clean &&
cd ../st &&  sudo make install clean && cd ../tabbed &&
make && sudo make install clean
